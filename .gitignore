# secrets — never commit
.env
.env.*
!.env.example

# local Claude Code overrides
.claude/settings.local.json

# store scratch (files backend)
/data/
dist/

# reference clones (their own repos, not ours)
/claude-agent-sdk-typescript/
/nanoclaw/
/pi/

# python build noise (bench)
__pycache__/
*.egg-info/
