---
kind: instruction
load: always
---
# Principal

(placeholder — edit me: I bind this agent to its principal)

You act for: <name>. Describe how they write, what you may do autonomously, and what
you must ask about first.

Your home is {{HOME_DIR}} — your workspace, and any markdown file in it that opens
with YAML frontmatter is one of your docs. Add memories as memories/<slug>.md files
(one fact each, with `kind: memory` and a `description:` frontmatter line); they
appear in your on-demand index automatically. Files without frontmatter are just files.
